# Core backend
fastapi>=0.110
uvicorn[standard]>=0.27
python-multipart>=0.0.9
pyyaml>=6.0
httpx>=0.27

# Lets FastAPI evaluate `X | None` type hints on Python 3.9 (native on 3.10+).
# Recommended runtime is Python 3.11+; this keeps the app working on 3.9 too.
eval_type_backport>=0.2; python_version < "3.10"

# LLM (NVIDIA NIM is OpenAI-compatible). Optional at runtime: without NIM_API_KEY
# the app falls back to an offline heuristic engine.
openai>=1.0.0

# Optional extras (enable as needed):
# pytesseract          # screenshot OCR (also needs the tesseract binary)
# pillow               # image handling for OCR
# psycopg[binary]      # Postgres for production memory
# pgvector             # vector search over past cases
# celery[redis]        # durable follow-up scheduling
# playwright           # real-channel automation (see connectors/README.md)

# Dev/test
pytest>=8.0
pytest-asyncio>=0.23
