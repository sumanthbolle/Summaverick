import type { RouteDef, RouteMaker } from "../types";

/** Routes for the research area. Populated in its dedicated task. */
export function researchRoutes(_route: RouteMaker): RouteDef[] {
  return [];
}
