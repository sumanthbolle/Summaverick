import type { RouteDef, RouteMaker } from "../types";

/** Routes for the content area. Populated in its dedicated task. */
export function contentRoutes(_route: RouteMaker): RouteDef[] {
  return [];
}
